node_modules/
dist/
.vscode/
.DS_Store
npm-debug.log*

# Environment / secrets — never commit real keys
.env
.env.local
.env.*.local
.env.development.local
.env.production.local
!.env.example

# Build artifacts / scratch
.trailer-build/

# Large media assets — keep in a storage bucket / Git LFS, not in the repo
/Covers/
/Parallax Station/
/Rainbow Dealer/
/public/trailers/
*.ai.zip
.vercel
.env*

# Claude Code machine-local settings (permissions differ per machine)
.claude/settings.local.json
